# Техническое задание и Логика работы приложения (Specs)

## 1. Главный экран
- Приветствие, дата/время.
- Карточка "Создать новый объект".
- Список "Недавние объекты".
- Быстрый поиск (адрес, телефон, дата).

## 2. Раздел "Объекты"
Система фильтров: новизна, алфавит, дата обновления, тип объекта.

### Карточка объекта содержит:
- **Статус (Workflow):**
    - Новый, Предпросчет, Этап 1 готов, Этап 2 готов, Этап 3 готов, Завершен.
- **Данные:** Адрес, Код домофона, Заказчик (client_info), Тип объекта, Источник (source: Владимир/Другое).
- **Интерфейс:** TabBar (Этапы | Щит | LED | Слаботочка).
- **Файлы:** PDF проекта, фото, схемы.

## 3. Раздел "Просчеты" (Логика смет) - В РАЗРАБОТКЕ
### Функции:
- Создать предв. смету по шаблону.
- **Авторасчет:** Генерация сметы работ на основе материалов.
- **Интеграция:** Перенос данных из "Инженерной карты" (Щиты) напрямую в смету (автоматика и боксы).

## 4. Раздел "Итоговые сметы" и Финансы
1. **Для клиента:** Полное количество (quantity), кнопка "Оплачено".
2. **Для подрядчика:** Учет субподряда через `contractor_quantity`.
3. **Валюта:** Поддержка USD/BYN для каждой строки.

## 5. Статистика
- Выручка, расходы на подрядчиков, чистая прибыль.
- Статистика по источникам (например, сколько объектов привел "Владимир").

## 6. Инженерные сети (Инженерная карта)
Данные фиксируются для истории и автоматизации сметы.

### 6.1 Силовой щит (ShieldGroup):
- **Smart Logic:** Каждое устройство имеет тип (Автомат, Диф и т.д.), Номинал и Полюса.
- **Нормализация:** Любой ввод (2п, 3пол) автоматически очищается через RegEx и приводится к стандарту (2P, 16A).
- **Расчет модулей:** Система автоматически вычисляет `modules_count` на основе полюсов и типа устройства.
- **Подбор щита:** Сумма модулей сопоставляется со стандартным рядом: [1, 2, 4, 6, 8, 12, 18, 24, 36, 48, 60, 72, 96, 108, 144]. Выбирается ближайший больший размер.

### 6.2 Зоны LED (LedZone):
- Фиксация трансформаторов и зон.
- **Авто-щит LED:** Расчет размера бокса на основе количества зон:
    - 1-4 транса -> 24 мод.
    - 5-8 трансов -> 36 мод.
    - 9-12 трансов -> 48 мод.
    - 13-17 трансов -> 60 мод.

### 6.3 Слаботочка и Интернет:
- Учет количества линий витой пары.
- Заметки по оборудованию (роутер, коммутатор).
- Рекомендация мультимедийного щита.

### 6.4 Шаблоны:
- Кнопка "Заполнить по шаблону" копирует набор строк из справочника в текущий проект.