# План доработки UI раздела "Справочник" (итерация 3)

## Цель
Внести дополнительные правки UX: синхронизация в `Каталог`, нейминг вкладки, tooltip на втором уровне, обновление визуала форм, унификация back-навигации.

## Выполненные шаги
1. Добавлена sync-кнопка в AppBar для `Каталог` (дублирует действие вкладки `Система`).
2. Переименована вкладка `Системные` -> `Система` во всех relevant `NavigationBar`.
3. Добавлены tooltip для FAB `+` на 2-м уровне.
4. Обновлен стиль контролов в диалогах (`InputDecoration`, switch-блок), без изменения структуры шапки/кнопок действий.
5. На 2-м уровне заменена кнопка назад на `arrow_back_ios_new` как на первом уровне.
6. Прогон `flutter analyze` для целевого файла.

## Статус
Все пункты выполнены.
